[project]
name = "cci-adapter-ornek"
version = "0.1.0"
requires-python = ">=3.12"

[project.entry-points."cci.adapters"]
ornek = "cci_adapter_ornek:build"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cci_adapter_ornek"]
